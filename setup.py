from setuptools import setup

setup(name = 'options',
      version = '0.1.0',
      description = 'Package for options analysis',
      author='Stephon Henry-Rerrie',
      author_email='srerrie@gmail.com',
      packages = ["options"]
)